import Phaser from 'phaser'
import { SceneKeys } from '.'

export default class Game extends Phaser.Scene
{
    constructor()
    {
        super(SceneKeys.Game)
    }

    init()
    {
        // TODO what are there in the board
    }

    create()
    {   
        //TODO create the cards layout first
    }
}